# Markdown package entry example

This module is imported via package path fallback: `import("examples/import_markdown_pkg")`.

```disturb
counter = 0,
counter++,
return {
  tag = "md_pkg",
  count = counter
},
```
