x = 5;
println(x.add(2, 3));

println(x.mul(2));

s = "ab";
println(s.upper());

println("hi,there".split(","));

arr = {a = "hi", b = "there"};
println(arr.join("-"));

println("  trim  ".trim());

println("hello".replace("hello", "hi"));

o = {a = 1, b = 2};
println(o.keys());

println(o.values());

println(o.has("a"));

println(o.delete("a"));

println(o.has("a"));

n = [1, 2];
n.push(3);
println(n);

println(n.pop());

n.insert(1, 9);
println(n);

println(n.remove(1));

str = "ab";
str.push("c");
println(str);

println(str.remove(1));
println(str);
