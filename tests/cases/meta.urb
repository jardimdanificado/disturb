obj = {alpha = "test", beta = [1, 2, 3]},

println(obj.name),
println(obj.type),
println(obj.size),
println(obj.capacity),

obj.size = 1,
println(obj.size),

obj.capacity = 4,
println(obj.capacity),

println(global.name),
println(global.name[0]),

s = "ab",
c = 'x',
println(s.type),
println(c.type),
