# Math module in Markdown

This module is authored in Markdown and exported from a fenced block.

```papagaio
secret = 17,
module_only_md = 456,
return {
  tag = "math_md",
  add = (a, b){ return a + b, },
  secret = secret
},
```
